# Website

本仓库仅包含以下内容：

- `frontend/`：React + Vite 前端代码
- `backend/`：Spring Boot 后端代码

## 快速启动

### 启动后端

```bash
cd backend
mvn spring-boot:run
```

默认地址：`http://localhost:9002`

### 启动前端

```bash
cd frontend
npm install
npm run dev
```

默认地址：`http://localhost:9001`
